export * from '@/core/css';

import {
    addStyles,
    removeStylesByProps,
    resetStyles,
    load,
    loadAsync,
    getAllStyles,
} from '@/core/css';

const CSS = {
    addStyles,
    removeStylesByProps,
    resetStyles,
    load,
    loadAsync,
    getAllStyles,
};

export default CSS;
